// src/index.js

const AsyncZlib = require("./libasync");
const { Extension, Language, OrderOptions } = require("./const");

module.exports = {
  AsyncZlib,
  Extension,
  Language,
  OrderOptions,
};
